%h1 Edit Post
.text-center
	= image_tag @post.image.url(:medium)
.form-wrapper

= render "form"

= link_to "Delete Post", post_path(@post), method: :delete, data: {confirm: "Are you sure you want delete this post"}
|
= link_to "cancel", posts_path